# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
next-env.d.ts

# env (secrets) — .env.example COMMIT edilir, gerisi ASLA (.bak/.local vb. dahil hepsi)
.env
.env.*
!.env.example

# claude code — proje settings.json COMMIT, local (token) ASLA
.claude/settings.local.json

# vercel
.vercel

# serwist (PWA build output — kaynak src/app/sw.ts)
/public/sw.js
/public/sw.js.map
/public/swe-worker-*.js

# supabase local
/supabase/.temp
/supabase/.branches

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo
.gstack/

# yerel kopya/çöp klasör (içinde .env.local var, asla commit edilmemeli)
projepazar-sistem/

# diagram-design skill (1.8MB assets) — proje-scope, commit etme
.claude/skills/*
!.claude/skills/kolayseo/

# İzole geliştirme worktree'leri (repoya girmez)
.worktrees/

# araştırma çıktıları: RAPOR/analiz (.md) ve elle toplanan kaynak veri (.json) COMMIT edilir.
# Scriptten yeniden üretilebilen tablolar ve ham HTML repoda tutulmaz (462 MB+).
/arastirma/**/*.csv
/arastirma/**/*.xlsx
/arastirma/**/ham/
/arastirma/**/*.log
