# 07 — Business Invariants (Projedar)

> Status: MIXED · Last verified: 2026-08-19 · Confidence: High
> Evidence: `db/*.sql`, `MASTER-PROJEDAR-IDENTITY-V2.md`, kod denetimi (2026-08-14, 2026-08-19)
> Her invariant: ID · ifade · rule class · enforcement katmanı · test durumu.
> **Not:** Bugün otomatik test YOK (doc 10). "Test: ✗" = invariant kodda korunuyor ama regresyon testi yok.

| ID | Invariant | Class | Enforcement (kanıt) | Test |
|---|---|---|---|---|
| INV-OPT-001 | Bir birim aynı anda iki çakışan aktif opsiyon tutamaz | PLATFORM INVARIANT | DB partial unique `opsiyon_tek_aktif on opsiyon(birim_id) where durum in('opsiyonlu','satis_beklemede')` + `opsiyon_talep_onayla` FOR UPDATE + re-check `durum='musait'` (`db/2026-08-05_opsiyon-yasam-dongusu.sql:91-92`) | ✗ |
| INV-OPT-002 | Emlakçı opsiyonu doğrudan INSERT edemez; yalnız SECURITY DEFINER RPC | PLATFORM INVARIANT | RLS `opsiyon_insert with check(is_admin())` (`db/2026-06-29_opsiyon-talep-onay.sql:18`) | ✗ |
| INV-OPT-003 | Opsiyon yöntemi projeye göre (dogrudan/onay/geçici) zorlanır; yanlış yöntem RPC reddedilir | COMMERCIAL RULE | RPC gate `proje.opsiyon_ayar.yontem` (`db/2026-08-13_opsiyon-admin-bypass.sql:8-54`) | ✗ |
| INV-OPT-004 | Süresi dolan opsiyon serbest bırakılır (kilit_bitis / dogrulama_bitis) | PLATFORM INVARIANT | pg_cron 15dk (schedule `db/2026-08-05_opsiyon-yasam-dongusu.sql:58`) → tek-yetkili fonksiyon `opsiyon_serbest_birak` (`db/2026-08-19_opsiyon-expiry-tek-yetkili.sql`); bkz INV-CRON-001 | ✗ |
| INV-CRON-001 | Expiry-driven opsiyon serbest bırakma TEK yetkili implementasyona sahiptir: DB fonksiyonu `opsiyon_serbest_birak()`. Her scheduler (pg_cron primary 15dk + Vercel günlük failsafe) bu fonksiyonu ÇAĞIRIR; seçim/silme/etiket mantığını asla yeniden yazmaz. Fonksiyon `DELETE … RETURNING`-tabanlı → audit yalnız gerçekten silinen satır için yazılır → eşzamanlı çalışmada idempotent (çift-event yok, çift-delete row-lock ile no-op). | PLATFORM INVARIANT | `db/2026-08-19_opsiyon-expiry-tek-yetkili.sql` (DELETE…RETURNING+event insert, `grant execute … to service_role`); Vercel `src/app/api/cron/_lib/isler.ts:148` yalnız `rpc('opsiyon_serbest_birak')` + count>0 anomaly log. N10/`666675c` | ✗ |
| INV-CRON-002 | Expiry audit etiket sözlüğü tek-kaynaklı: `dogrulandi=false` penceresi lapse → `dogrulama_sure_doldu`; kesin-kilit lapse → `sure_doldu`. Tüketici (güven skoru RPC `dusen` sayımı) bu sözlüğe bağımlı; ikinci bir yazar farklı etiket basamaz. | PLATFORM INVARIANT | tek CASE `db/2026-08-19_opsiyon-expiry-tek-yetkili.sql:26-27`; consumer `db/2026-08-05_guven-skoru.sql:47,58` | ✗ |
| INV-TAHSIS-001 | Tahsis = görünürlük/dağıtım ilişkisi, stok ownership DEĞİL | PLATFORM INVARIANT | `birim` tek referans; `tahsis` ayrı tablo (V2 A.2/D.17) | ✗ |
| INV-TAHSIS-002 | Askıda/kaldırılmış tahsis emlakçıya birim görünürlüğü VERMEZ | PLATFORM INVARIANT | `emlakci_birim_gorebilir` `t.durum='aktif' AND baslangic<=now() AND (bitis is null OR bitis>now())` (`db/2026-08-12_tahsis-yasam-dongusu.sql:41-58`) | ✗ |
| INV-TAHSIS-003 | Kaldırılmış (kaldirildi) tahsis terminal; tekrar geçiş yapılamaz | PLATFORM INVARIANT | `tahsis_toplu` `durum<>'kaldirildi'` filtresi (`:149,178`) | ✗ |
| INV-TAHSIS-004 | Tahsis kapsam AKIBET ÖNİZLEMESİ salt-okunur + owner-guard'lı + `birim_kapsaminda` kaynaklı (RLS görünürlüğüyle özdeş sayım). Denominatör = `satilabilir=true AND ana_birim_id is null` = danışmanın satılabilir yüzeyleriyle (eslestir/dalga/katalog/müsait grid) birebir; eklenti+non-satilabilir birim SAYILMAZ (bilinçli, "açılan satılabilir birim" dürüst sayı). Çakışma önizlemesi `tahsis_munhasir_cakisma` = server enforcement ile aynı kaynak. Server enforcement (`tahsisEkle`) DEĞİŞMEZ; önizleme sadece bilgilendirir. | PLATFORM INVARIANT (read model) | `db/2026-08-19_tahsis-kapsam-ozet.sql` (`tahsis_kapsam_ozet`, security definer, `_tahsis_proje_sahibi` gate → non-owner `{yetki:false}`, count `birim_kapsaminda`); client `src/app/uretici/proje/[id]/TahsisForm.tsx:77-101,318-333`; parity: `src/app/danisman/eslestir/page.tsx:21,24` + `danisman/proje/[id]/page.tsx:167,196` | ✗ |
| INV-RLS-001 | Emlakçı yalnız kendine tahsisli + KYC-dogrulandi + kapsam-içi (veya demo) birimi görür | PLATFORM INVARIANT | SECURITY DEFINER `emlakci_birim_gorebilir` (6-arg) + KYC gate | ✗ |
| INV-RLS-002 | Service-role yalnız server/cron; client bundle'a girmez (`NEXT_PUBLIC` değil) | PLATFORM INVARIANT | V2 D.6; `createAdminClient` server-only | ✗ |
| INV-RLS-003 | Non-admin kendini `belge_durumu='dogrulandi'` yapamaz (KYC-gate bypass kalkanı) | PLATFORM INVARIANT | trigger `belge_durumu_guard` (doc 02:94) | ✗ |
| INV-PRICE-001 | Fiyat/durum tek referans `birim`; paylaşım/katalog/mikrosite canlı basar, kopyalamaz | PLATFORM INVARIANT (DEĞİŞMEZ #2) | `/p/{kod}` request-time service-role read (`src/lib/sharing.ts:144-161`) | ✗ |
| INV-PRICE-002 | WhatsApp metni ve OG görseline fiyat basılmaz (cache donması) | PRODUCT DECISION | V2 A.2 | ✗ |
| INV-LEAD-001 | Lead `atanan_id=ilk_paylasan_id`=paylaşan emlakçı (ilk kaydeden) | COMMERCIAL RULE | `src/app/api/lead/route.ts:74-75` | ✗ |
| INV-LEAD-002 | Aynı `(telefon_norm, birim_id)` 10dk içinde ikinci lead oluşturmaz (throttle) | CONFIGURABLE POLICY | `api/lead/route.ts:44-59` (429) — **not durable cross-agent claim** | ✗ |
| INV-LEAD-003 | Müteahhit toplu lead feed'i görmez; yalnız ad/telefon birebir sorgu | PLATFORM INVARIANT | `db/2026-07-24_lead-select-rls.sql` (producer arm removed) | ✗ |
| INV-COMM-001 | Müteahhidin danışmana tanımladığı satış komisyonundan Projedar pay ALMAZ | PRODUCT DECISION | V2 Bölüm C; `tahsis.komisyon_tip/deger` emlakçıya RLS-görünür | ✗ |
| INV-COMM-002 | Hakediş = **canlı-hesaplanan salt-bilgi mutabakat görünümü**, Projedar depo/defter DEĞİL: (a) "ödendi/ödenmedi" (payment/odendi) durumu YOK — Projedar ödemeye taraf değil, tahsilat/alacak takibi tutmaz; (b) danışman hakedişinde **salt-okunur** + yalnız **kendi** kaydını görür (own-only); (c) tutar `birim_satici_kazanci` ile canlı türetilir (liste_fiyatı×tahsis komisyonu), snapshot satırı yazılmaz, ham oran client'a çıkmaz (yalnız TL). İki durum: Potansiyel (opsiyonlu, satılmamış · tahmini) vs Hesaplanan (sonuc=satildi). | PLATFORM INVARIANT (DEĞİŞMEZ) | Option L (DDR-014, `37baf38`): `src/app/danisman/hakedis/page.tsx` own-client + `.eq(satici_id,uid)` + RPC `p_satici=auth.uid()` authz-gate; `birimSatisKapat`'tan hakediş upsert KALDIRILDI; `hakedis` tablosu bilinçli uygulanmadı (DEBT-009). Ayna: `/uretici/hakedis` de canlı-hesap. | ✗ |
| INV-FRESH-001 | Her yazışta `son_guncelleme=now()`; 15g+ birim `stale=true` | PLATFORM INVARIANT (DEĞİŞMEZ #5) | daily cron `freshnessCalistir` (V2 A.3) | ✗ |
| INV-EVENT-001 | `events` append-only; INSERT yalnız service-role/SECURITY DEFINER | PLATFORM INVARIANT | doc 04:73 (no client INSERT policy) | ✗ |
| INV-SHARE-001 | Geçersiz HMAC token → `notFound()`; fallback yok | PLATFORM INVARIANT | `src/lib/sharing.ts` (LEAD_SHARE_SECRET, no fallback) | ✗ |
| INV-SHARE-002 | `paylasim_kod.aktif=false` = **KASITLI REVOKE** (hard 404 + lead/etkileşim reddi, DDR-015 uçtan-uca kimlik). Bir birim `satildi` olunca kod `aktif=false` YAPILMAZ; satış-sonrası mikrosite bozulması `birim.durum`-güdümlüdür (render "Satıldı + benzer daireler"e düşer, `/api/lead` N11 durum-kapısıyla reddeder). `aktif` tek anlam taşır (revoke); satış ile revoke aynı boole'yi PAYLAŞMAZ → ters re-share niyeti çakışması yok. Trigger/`pasif_nedeni` makinesi KURULMADI (MODE A DDR: degrade-not-kill, owner kabul 2026-08-20). | PLATFORM INVARIANT (DDR, owner 2026-08-20) | Karar: sale `paylasim_kod`'a DOKUNMAZ (`src/app/uretici/actions.ts` `birimSatisKapat`); sold-degrade `src/app/p/[...slug]/page.tsx:336,676,694,702` (durum-güdümlü); OQ-SHARE-001 kapanışı | ✗ |
| INV-SHARE-003 | Mikrosite-yetkili her public-write API'si canlı `birim.durum`'u server-side kapı olarak okur (kod `aktif`'ten BAĞIMSIZ). Satılan/terminal birimde lead+anonim etkileşim (favori/ödeme) YAZILMAZ. | PLATFORM INVARIANT | `/api/lead` ✓ N11 (`src/app/api/lead/route.ts:68-83` `birimLeadKabulEdilebilir`); `/api/etkilesim` ✓ (`src/app/api/etkilesim/route.ts:38-43`, commit `0593733`) | ✗ |
| INV-ADMIN-001 | admin = platform işletmecisi; stok/birim/fiyat düzenlemez (panelde yok) | PLATFORM INVARIANT | Sistem-Kurallari B.1; admin panelinde stok CRUD yok | ✗ |
| INV-SCOPE-001 | Projedar CDO'nun agent/skill/rules/knowledge/kararları YALNIZ Projedar reposunda geçerli; başka Claude projesinin davranışını değiştiremez, global config'e taşınmaz, `memory: project` dışında memory kullanılmaz | PLATFORM INVARIANT | agent scope guard + `git rev-parse --show-toplevel` doğrulaması; global `~/.claude` dokunulmaz | ✗ |
| INV-ADMIN-002 | admin canlı (non-demo) birimde `opsiyon.satici_id`/satıcı OLAMAZ; concierge aksiyonu scope'lu + `kaynak='admin_concierge'` işaretli olmalı | PLATFORM INVARIANT | **İHLAL:** `db/2026-08-13_opsiyon-admin-bypass.sql:14,30` `is_admin() OR` ile açıyor → audit E1/F1a | ✗ |
| INV-AUDIT-001 | Her state-değiştiren admin mutasyonu `events`'e satır yazar (actor + before/after); "denetim" UI bütünlük iddiası buna bağlı | PLATFORM INVARIANT | **FIX (E2):** `tip="hesap"` audit → kullaniciGuncelle (before/after, rol→admin `yuksek_riskli`), kullaniciOlustur, ureticiEkle (dogrulanmis_otomatik), ofisEkle, paketEkle/Duzenle/Sil; denetim UI'da görünür/filtrelenir | ✗ |
| INV-SALE-001 | Tüm "→satıldı" geçişleri tek satış-kapama yolundan (satıcı attribution = aktif opsiyon sahibi; hakediş satırı YAZILMAZ, canlı-hesap — DDR-014/INV-COMM-002); grid'de ham `durum=satildi` yasak | PLATFORM INVARIANT | **FIX (C1, `c5e7165`+):** `birimDurumGuncelle`/`birimTopluGuncelle` ham `satildi` reddeder → `birimSatisKapat` zorunlu; toplu seçenekten kaldırıldı. Attribution = opsiyon `sonuc=satildi`+`satici_id` (Option L'de hakediş upsert `37baf38` ile kaldırıldı). Açık istisna: Excel import "zaten satılmış" (doğrudan satış, danışman yok — meşru initial-load) | ✗ |
| INV-STATE-001 | Hiçbir cron path `satis_beklemede` birimi serbest bırakamaz (sözleşme aşaması korunur) | PLATFORM INVARIANT | **FIX (B3):** iki cron yolu da (`isler.ts` + pg_cron `opsiyon_serbest_birak`) yalnız `opsiyonlu` serbest bırakır; `satis_beklemede` müteahhit teyidine (birimSatisKapat) veya manuel geri-alışa bağlı | ✗ |
| INV-EXCL-001 | Münhasır tahsis kapsamı, aynı birimlerde başka aktif tahsisle çakışamaz (veya açık override+audit) | COMMERCIAL RULE | **FIX (B4):** `tahsis_munhasir_cakisma` RPC + `tahsisEkle`/`tahsisGuncelle` server enforce (override yoksa reddet) + client confirm + override audit. Karar: blok değil, uyar+açık override | ✗ |
| INV-PRICEVIS-001 | Fiyat-görünürlük RPC'leri authenticated çağrıda advisor kimliğini `auth.uid()`'ten türetir; client'ın geçtiği advisor param YALNIZ service-role (mikrosite) için güvenilir → `coalesce(auth.uid(), p_emlakci)`, asla tersi | PLATFORM INVARIANT | `db/2026-08-14_fiyat-redaksiyon-tek-kaynak.sql` (MODE B P1 düzeltmesi); `birim_gorunur_fiyat` authenticated'a grant EDİLMEZ | ✗ |
| INV-KYC-001 | Emlakçı `belge_durumu='dogrulandi'` (canlı-stok güven kapısı) tek yoldan verilir: `belgeKarar` onay. Zorunlu belge seti = **yalnız `mesleki_yeterlilik` (MYS)** eksikse VEYA herhangi belge AI `gecerli===false` ise tek-tık onay BLOKLANIR; admin ancak `override=1` + `gerekce.length>=5` ile onaylar (soft-block, hard-block değil — N6 owner kararı). MYS-varlık kontrolü app-layer POLICY; DB-invariant değil (bkz INV-RLS-003 hard-gate). Başka hiçbir kod yolu emlakçı belge_durumu'nu dogrulandi yapmaz (tek chokepoint). | COMMERCIAL RULE (policy gate) | `src/app/admin/actions.ts:146` (`ROL_ZORUNLU_BELGE`), `:188-214` gate; grep-doğrulandı tek write path `:217` | ✗ |
| INV-KYC-002 | Kanıt tabanı: MYS dosyası eksik (`eksik.includes('mesleki_yeterlilik')`) iken override yapılıyorsa `profil_detay.mys_belge_no` beyan edilmiş OLMALI; yoksa override reddedilir (dayanaksız onay yok). MYS dosyası MEVCUT ama AI-flagged ise floor uygulanmaz (admin dosyayı görüp gerekçelendirir). | COMMERCIAL RULE (evidence floor) | `src/app/admin/actions.ts:207-212` | ✗ |
| INV-UI-001 | UI, RLS gate'inin VERMEDİĞİ bir yeteneği/görünürlüğü iddia edemez; rol/doğrulama kopyası ve etiketi `rol`+`belge_durumu`'ndan TÜRETİLİR, asla hard-code edilmez. Örnek ihlal (N3 öncesi): admin-provisioned org rolüne (ofis_yetkili, `belge_durumu='yok'` → RLS demo-only) "doğrulanmadı, Belgeni yükle" + hard-code "Onaylı Danışman" gösteriliyordu (yanlış remedy + rol yanlış + banner ile çelişki). | PLATFORM INVARIANT | **FIX (N3, `e1d4cc1`):** `src/app/danisman/layout.tsx` `dogrulanmadi = !adminMi && !orgRol && belge_durumu!=='dogrulandi'`; org banner `/danisman/dogrulama` linki YOK; etiket `ROL_KISI_ETIKET` (`src/lib/roller.ts`, ORG_ROLLER); emlakçı `dogrulandi?"Onaylı Danışman":"Danışman"`. RLS DEĞİŞMEDİ. Bkz. OQ-ORGROLE-001 | ✗ |

## Aday invariant kararları (spec §55 → ACCEPT/MODIFY)
- INV-CANDIDATE-001 → **ACCEPT** = INV-OPT-001.
- INV-CANDIDATE-002 → **ACCEPT** = INV-TAHSIS-001.
- INV-CANDIDATE-003 (suspended tahsis yeni ticari taahhüt yaratamaz) → **MODIFY:** yeni görünürlük/opsiyon engellenir (INV-TAHSIS-002) ama **mevcut aktif opsiyon serbest bırakılmaz** (gap — bkz. `references/17` RISK-TAHSIS-001, `references/22` DEBT-001).
- INV-CANDIDATE-004 (fiyat değişimi geçmiş rezervasyon terms'ini sessizce değiştirmez) → **DECISION REQUIRED:** bugün opsiyon satırı fiyat snapshot TAŞIMAZ; mikrosite canlı basar → orta-opsiyon fiyat değişimi müşteriye gösterileni sessizce değiştirir (bkz. `references/17` RISK-PRICE-001, OQ-PRICE-001).
- INV-CANDIDATE-005 (public görünürlük ≠ private stok) → **ACCEPT:** public'te canlı stok sayısı/fiyat gösterilmez (V2 D.27).
- INV-CANDIDATE-006 (yetki kaybı → stale session ayrıcalık taşımaz) → **ACCEPT (partial):** `proxy.ts` her istekte `getUser()` server-doğrular; RLS `durum='aktif'` gate. Tahsis-revoke→opsiyon gap ayrı (RISK-TAHSIS-001).
- INV-CANDIDATE-007 (ticari override attributable/auditable) → **ACCEPT (partial):** `tahsis_toplu` events'e old/new yazar; ama admin opsiyon-bypass audit netliği OQ-AUDIT-001.
